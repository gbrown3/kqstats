/**
 * Some constants useful for GameStats
 */
export abstract class GameConstants {

    // Sourced from https://github.com/killer-queen-stats/kqstats/wiki/Map-Coordinates
    // The y val is a rough estimate, but x should be correct
    static readonly MAP_CENTER = {
        'x': 960,
        'y': 500
    };
}